# MariMail Client

Next.js 14 App Router frontend for MariMail.

Run from the repository root:

```bash
pnpm dev
```

The client runs on http://localhost:3000. The backend runs from `server` on http://localhost:3001.
