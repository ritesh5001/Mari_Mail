# Project Documentation

This folder keeps human-facing project documents out of the repository root.

- `project-context.md` contains the working project context and phase summary.
- `MariMail Plan.docx` is the full product plan and source specification.
- `.claude/` in the repository root contains hidden automation prompts and extracted phase instructions.
